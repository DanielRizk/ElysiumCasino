package org.daniel.elysium.ultimateTH.constants;

public enum UthGameStage {
    /** The initial stage of the game. */
    START,

    /** The phase where the first 3 cards are exposed. */
    FLOP,

    /** The phase where the 4th card is exposed. */
    RIVER,

    /** The phase where the 5th card is exposed. */
    TURN,

    /** The final stage of the game. */
    FINAL
}
